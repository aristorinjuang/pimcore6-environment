server {
    listen 80;
    root /var/www/pimcore6;
    index index.php;
    server_name 6.pimcore.local;

    location / {
        try_files $uri $uri/ =404;
    }

    location ~ \.php$ {
        include snippets/fastcgi-php.conf;
        fastcgi_pass unix:/run/php/php7.2-fpm.sock;
    }
}